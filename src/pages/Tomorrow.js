import React from 'react'
import styled from 'styled-components/macro'

const Tomorrow = () => {
  return <Wrapper>Tomorrow</Wrapper>
}

export default Tomorrow

const Wrapper = styled.section`
  border: 1px solid #fff;
`
