import React from 'react'
import styled from 'styled-components/macro'

const Temperature = () => {
  return <Wrapper>Temperature</Wrapper>
}

export default Temperature

const Wrapper = styled.section`
  border: 1px solid white;
`
